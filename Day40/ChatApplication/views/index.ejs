<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>mini Whatsapp</title>
  <link rel="stylesheet" href="/style.css">

</head>

<body>
    <h2>All Chats</h2>
    <form action="/chat/new" method="get">
        <button class="btn">New Chat</button>
    </form>
    <% for(chat of allChats){ %>
        <div class="chatbox">
            <p>From: <i><%= chat.from %></i> </p>
            <div class="msg"><p> <%= chat.msg %> </p>
                <section class="inline">
                    <form action="/chats/<%=chat._id %>/edit"> <button class="btn">Edit Chat</button></form>
                    <form action="/chats/<%=chat._id %>?_method=DELETE" method="post"> <button class="btn">Delete</button></form>
                </section>
            </div>
            <p>To: <i><%= chat.to %></i> </p>
            <p class="date"> <%= chat.created_at.toString().split(" ")[4]%> </p>
            <p class=" date"> <%= chat.created_at.toString().split(" ").slice(0,4).join(" -")%> </p>
        </div>
        <br><br>

    <% } %>

</body>

</html>